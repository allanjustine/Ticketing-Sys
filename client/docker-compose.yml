version: '3.8'

services:
  ticketing-netsuite-client:
    build: .
    container_name: ticketing-netsuite-client
    restart: unless-stopped
    ports:
      - "7030:7030"
    volumes:
      - /home/webserver/systems/Ticketing-Sys/client/.env:/var/www/.env